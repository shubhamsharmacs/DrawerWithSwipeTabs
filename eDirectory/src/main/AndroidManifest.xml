<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.arcasolutions"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="16" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <permission
        android:name="com.arcasolutions.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />
    <uses-permission android:name="com.arcasolutions.permission.MAPS_RECEIVE" />

    <application
        android:name="com.arcasolutions.App"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.arcasolutions.ui.activity.MapActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.activity.CategoryResultActivity" />

        <activity android:name=".ui.activity.listing.ListingResultActivity" />

        <activity android:name=".ui.activity.listing.ListingDetailActivity" />

        <activity android:name=".ui.activity.listing.DealResultActivity" />

        <activity android:name=".ui.activity.article.ArticleResultActivity" />

        <activity
            android:name=".ui.activity.article.ArticleDetailActivity"
            android:hardwareAccelerated="false" />

        <activity android:name=".ui.activity.classified.ClassifiedResultActivity" />

        <activity android:name=".ui.activity.classified.ClassifiedDetailActivity" />

        <activity android:name=".ui.activity.event.EventResultActivity" />

        <activity android:name=".ui.activity.event.EventDetailActivity" />

        <activity android:name=".ui.activity.GalleryViewActivity" />

        <activity android:name=".ui.activity.SettingActivity" />

        <activity android:name=".ui.activity.LoginActivity"
            android:launchMode="singleTop" />

        <activity android:name=".web.FacebookWebOAuthActivity"
            android:excludeFromRecents="true"
            android:noHistory="true"/>

        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="@string/androidApiKey" />

    </application>

</manifest>
